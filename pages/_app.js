import "../styles/globals.css";
import { Global, Loading, Page, DemoPanel, Dot, Error } from "~/src/styles";

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
